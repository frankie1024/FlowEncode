<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="FlowEncode.Controls.Settings.SetupGuideOverlayView"
    x:Name="RootView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:shared="using:FlowEncode.Controls.Shared"
    xmlns:vm="using:FlowEncode.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
        <Grid Background="{ThemeResource AppModalSmokeBrush}">
        <Grid Padding="{StaticResource AppCardPadding}">
            <Border
                x:Name="SetupGuideDialogCard"
                MaxWidth="1160"
                Padding="{StaticResource AppDialogPadding}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{ThemeResource CardBrush}"
                BorderBrush="{ThemeResource ThemeBorderElevatedBrush}"
                BorderThickness="{StaticResource AppCardBorderThickness}"
                CornerRadius="{StaticResource AppLargeCornerRadius}"
                shared:UiElevation.Level="E3">
                <Grid RowSpacing="{StaticResource AppSpacingL}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Grid x:Name="SetupGuideHeaderPanel" ColumnSpacing="{StaticResource AppSpacingL}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Spacing="{StaticResource AppSpacingS}">
                            <TextBlock FontFamily="{StaticResource AppDisplayFont}" FontSize="{StaticResource AppTitleFontSize}" FontWeight="SemiBold" Text="{Binding Texts.SetupGuideTitle}" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="{StaticResource AppSpacingS}" VerticalAlignment="Top">
                            <ProgressRing Width="24" Height="24" IsActive="True" Visibility="{Binding SetupGuideActionProgressVisibility}" />
                            <Button AutomationProperties.Name="{Binding SetupGuideRefreshActionText}" Click="RefreshSetupGuideButton_Click" IsEnabled="{Binding CanExecuteSetupGuideRefreshAction}" ToolTipService.ToolTip="{Binding SetupGuideRefreshActionText}">
                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                    <SymbolIcon Symbol="Refresh" />
                                    <TextBlock Text="{Binding SetupGuideRefreshActionText}" />
                                </StackPanel>
                            </Button>
                            <Button AutomationProperties.Name="{Binding SetupGuideUpdateCheckActionText}" Click="CheckSetupDependencyUpdatesButton_Click" IsEnabled="{Binding CanExecuteSetupGuideUpdateCheckAction}" ToolTipService.ToolTip="{Binding SetupGuideUpdateCheckActionText}">
                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                    <SymbolIcon Symbol="Find" />
                                    <TextBlock Text="{Binding SetupGuideUpdateCheckActionText}" />
                                </StackPanel>
                            </Button>
                            <Button AutomationProperties.Name="{Binding Texts.SetupGuideCloseButton}" Click="CloseSetupGuideButton_Click" Style="{StaticResource AccentButtonStyle}" ToolTipService.ToolTip="{Binding Texts.SetupGuideCloseButton}">
                                <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                    <SymbolIcon Symbol="Forward" />
                                    <TextBlock Text="{Binding Texts.SetupGuideCloseButton}" />
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <FlipView
                        x:Name="SetupGuideFlipView"
                        Grid.Row="1"
                        VerticalAlignment="Top"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        ItemsSource="{Binding SetupGuideCards}"
                        SelectedIndex="{Binding SelectedSetupGuideCardIndex, Mode=TwoWay}"
                        SelectionChanged="SetupGuideFlipView_SelectionChanged">
                        <FlipView.ItemTemplate>
                            <DataTemplate x:DataType="vm:SetupGuideCardViewModel">
                                <Border Margin="{StaticResource AppSetupGuideCarouselCardMargin}" Padding="{StaticResource AppCardPadding}" VerticalAlignment="Stretch" Background="{ThemeResource CardAltBrush}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="{StaticResource AppCardBorderThickness}" CornerRadius="{StaticResource AppLargeCornerRadius}">
                                    <Grid RowSpacing="{StaticResource AppSpacingL}">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                        </Grid.RowDefinitions>

                                        <StackPanel Spacing="{StaticResource AppSpacingS}">
                                            <TextBlock FontFamily="{StaticResource AppDisplayFont}" FontSize="{StaticResource AppHeadlineFontSize}" FontWeight="SemiBold" Text="{Binding Title}" />
                                        </StackPanel>

                                        <ScrollViewer Grid.Row="1" MinHeight="0" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate x:DataType="vm:SetupGuideDependencyItemViewModel">
                                                        <Border Margin="{StaticResource AppBottomSpacingM}" Padding="{StaticResource AppInsetPanelPadding}" Background="{ThemeResource CardBrush}" BorderBrush="{ThemeResource CardBorderBrush}" BorderThickness="{StaticResource AppCardBorderThickness}" CornerRadius="{StaticResource AppCardCornerRadius}">
                                                            <Grid RowSpacing="{StaticResource AppSpacingM}">
                                                                <Grid.RowDefinitions>
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                    <RowDefinition Height="Auto" />
                                                                </Grid.RowDefinitions>

                                                                <Grid ColumnSpacing="{StaticResource AppSpacingL}">
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                    </Grid.ColumnDefinitions>

                                                                    <StackPanel Spacing="{StaticResource AppSpacingXS}">
                                                                        <TextBlock FontFamily="{StaticResource AppDisplayFont}" FontSize="{StaticResource AppSubsectionTitleFontSize}" FontWeight="SemiBold" Text="{Binding Title}" />
                                                                    </StackPanel>

                                                                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="{StaticResource AppSpacingS}" HorizontalAlignment="Right" VerticalAlignment="Top">
                                                                        <Button AutomationProperties.Name="{Binding DataContext.Texts.ManualSelectButton, ElementName=RootView}" Tag="{Binding Kind}" Click="ManualSelectSetupDependencyButton_Click" IsEnabled="{Binding ManualSelectEnabled}" Visibility="{Binding ManualSelectVisibility}" ToolTipService.ToolTip="{Binding DataContext.Texts.ManualSelectButton, ElementName=RootView}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                                                                <SymbolIcon Symbol="OpenFile" />
                                                                                <TextBlock Text="{Binding DataContext.Texts.ManualSelectButton, ElementName=RootView}" />
                                                                            </StackPanel>
                                                                        </Button>
                                                                        <Button AutomationProperties.Name="{Binding DataContext.Texts.ClearManualPinButton, ElementName=RootView}" Tag="{Binding Kind}" Click="ClearManualPinnedSetupDependencyButton_Click" IsEnabled="{Binding ClearManualPinEnabled}" Visibility="{Binding ClearManualPinVisibility}" ToolTipService.ToolTip="{Binding DataContext.Texts.ClearManualPinButton, ElementName=RootView}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                                                                <SymbolIcon Symbol="UnPin" />
                                                                                <TextBlock Text="{Binding DataContext.Texts.ClearManualPinButton, ElementName=RootView}" />
                                                                            </StackPanel>
                                                                        </Button>
                                                                        <Button AutomationProperties.Name="{Binding PrimaryActionText}" Tag="{Binding Kind}" Click="InstallSetupDependencyButton_Click" IsEnabled="{Binding PrimaryActionEnabled}" Visibility="{Binding PrimaryActionVisibility}" Style="{StaticResource AccentButtonStyle}" ToolTipService.ToolTip="{Binding PrimaryActionText}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                                                                <SymbolIcon Symbol="Download" />
                                                                                <TextBlock Text="{Binding PrimaryActionText}" />
                                                                            </StackPanel>
                                                                        </Button>
                                                                        <Button AutomationProperties.Name="{Binding DataContext.Texts.UninstallButton, ElementName=RootView}" Tag="{Binding Kind}" Click="UninstallSetupDependencyButton_Click" IsEnabled="{Binding UninstallActionEnabled}" Visibility="{Binding UninstallVisibility}" ToolTipService.ToolTip="{Binding DataContext.Texts.UninstallButton, ElementName=RootView}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                                                                <SymbolIcon Symbol="Delete" />
                                                                                <TextBlock Text="{Binding DataContext.Texts.UninstallButton, ElementName=RootView}" />
                                                                            </StackPanel>
                                                                        </Button>
                                                                        <HyperlinkButton AutomationProperties.Name="{Binding DataContext.Texts.ReleasePageButton, ElementName=RootView}" Tag="{Binding ReleaseUrl}" Click="OpenUrlButton_Click" Visibility="{Binding ReleaseVisibility}" ToolTipService.ToolTip="{Binding DataContext.Texts.ReleasePageButton, ElementName=RootView}">
                                                                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                                                                <SymbolIcon Symbol="Link" />
                                                                                <TextBlock Text="{Binding DataContext.Texts.ReleasePageButton, ElementName=RootView}" />
                                                                            </StackPanel>
                                                                        </HyperlinkButton>
                                                                    </StackPanel>
                                                                </Grid>

                                                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="{StaticResource AppSpacingS}" Visibility="{Binding ReadyBadgeVisibility}">
                                                                    <SymbolIcon Symbol="Accept" Foreground="{ThemeResource AppSuccessBrush}" />
                                                                    <TextBlock FontWeight="SemiBold" Foreground="{ThemeResource AppSuccessBrush}" Text="{Binding StatusTitle}" />
                                                                </StackPanel>

                                                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="{StaticResource AppSpacingS}" Visibility="{Binding WarningBadgeVisibility}">
                                                                    <SymbolIcon Symbol="Sync" Foreground="{ThemeResource AppWarningBrush}" />
                                                                    <TextBlock FontWeight="SemiBold" Foreground="{ThemeResource AppWarningBrush}" Text="{Binding StatusTitle}" />
                                                                </StackPanel>

                                                                <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="{StaticResource AppSpacingS}" Visibility="{Binding BlockedBadgeVisibility}">
                                                                    <SymbolIcon Symbol="Important" Foreground="{ThemeResource AppErrorBrush}" />
                                                                    <TextBlock FontWeight="SemiBold" Foreground="{ThemeResource AppErrorBrush}" Text="{Binding StatusTitle}" />
                                                                </StackPanel>

                                                                <StackPanel Grid.Row="2" Spacing="{StaticResource AppSpacingXS}">
                                                                    <TextBlock Text="{Binding StatusText}" TextWrapping="WrapWholeWords" />
                                                                    <TextBlock Foreground="{ThemeResource SubtleTextBrush}" Text="{Binding InstalledVersionText}" TextWrapping="WrapWholeWords" Visibility="{Binding InstalledVersionVisibility}" />
                                                                    <TextBlock Foreground="{ThemeResource SubtleTextBrush}" Text="{Binding LatestVersionText}" TextWrapping="WrapWholeWords" Visibility="{Binding LatestVersionVisibility}" />
                                                                </StackPanel>

                                                                <TextBlock Grid.Row="3" Foreground="{ThemeResource AppWarningBrush}" Text="{Binding WarningText}" TextWrapping="WrapWholeWords" Visibility="{Binding WarningVisibility}" />

                                                                <Grid Grid.Row="4" RowSpacing="{StaticResource AppSpacingS}" Visibility="{Binding ProgressVisibility}">
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="*" />
                                                                        <ColumnDefinition Width="Auto" />
                                                                    </Grid.ColumnDefinitions>

                                                                    <TextBlock Foreground="{ThemeResource SubtleTextBrush}" Text="{Binding InstallProgressText}" TextWrapping="WrapWholeWords" />
                                                                    <TextBlock Grid.Column="1" FontFamily="{StaticResource AppMonoFont}" Text="{Binding InstallProgressPercentText}" />
                                                                    <ProgressBar Grid.Row="1" Grid.ColumnSpan="2" Height="6" Minimum="0" Maximum="100" Value="{Binding InstallProgressPercent}" IsIndeterminate="{Binding IsInstallIndeterminate}" />
                                                                </Grid>

                                                                <TextBlock Grid.Row="5" FontFamily="{StaticResource AppMonoFont}" FontSize="{StaticResource AppCaptionFontSize}" Foreground="{ThemeResource SubtleTextBrush}" Text="{Binding ExecutablePath}" TextWrapping="WrapWholeWords" Visibility="{Binding PathVisibility}" />

                                                                <TextBlock Grid.Row="6" FontFamily="{StaticResource AppMonoFont}" FontSize="{StaticResource AppCaptionFontSize}" Foreground="{ThemeResource SubtleTextBrush}" Text="{Binding DetailText}" TextWrapping="WrapWholeWords" Visibility="{Binding DetailVisibility}" />
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </ScrollViewer>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </FlipView.ItemTemplate>
                    </FlipView>

                    <Grid x:Name="SetupGuideFooterPanel" Grid.Row="2" HorizontalAlignment="Center" ColumnSpacing="{StaticResource AppSpacingL}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            MinWidth="{StaticResource AppLargeCommandMinWidth}"
                            MinHeight="{StaticResource AppLargeCommandMinHeight}"
                            Padding="{StaticResource AppLargeCommandPadding}"
                            AutomationProperties.Name="{Binding Texts.SetupGuidePreviousButton}"
                            FontSize="{StaticResource AppLargeCommandFontSize}"
                            FontWeight="SemiBold"
                            Click="SetupGuidePreviousButton_Click"
                            IsEnabled="{Binding CanMoveSetupGuidePrevious}"
                            ToolTipService.ToolTip="{Binding Texts.SetupGuidePreviousButton}">
                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                <SymbolIcon Symbol="Back" />
                                <TextBlock Text="{Binding Texts.SetupGuidePreviousButton}" />
                            </StackPanel>
                        </Button>

                        <TextBlock
                            Grid.Column="1"
                            Margin="{StaticResource AppInlineGapM}"
                            VerticalAlignment="Center"
                            FontFamily="{StaticResource AppMonoFont}"
                            FontSize="{StaticResource AppSectionTitleFontSize}"
                            FontWeight="SemiBold"
                            Text="{Binding SetupGuidePositionText}"
                            Visibility="{Binding SetupGuidePositionVisibility}" />

                        <Button
                            Grid.Column="2"
                            MinWidth="{StaticResource AppLargeCommandMinWidth}"
                            MinHeight="{StaticResource AppLargeCommandMinHeight}"
                            Padding="{StaticResource AppLargeCommandPadding}"
                            AutomationProperties.Name="{Binding SetupGuideForwardButtonText}"
                            FontSize="{StaticResource AppLargeCommandFontSize}"
                            FontWeight="SemiBold"
                            Click="SetupGuideNextButton_Click"
                            IsEnabled="{Binding CanAdvanceOrDismissSetupGuide}"
                            Style="{StaticResource AccentButtonStyle}"
                            ToolTipService.ToolTip="{Binding SetupGuideForwardButtonText}">
                            <StackPanel Orientation="Horizontal" Spacing="{StaticResource AppCommandIconTextSpacing}">
                                <TextBlock Text="{Binding SetupGuideForwardButtonText}" />
                                <SymbolIcon Symbol="Forward" />
                            </StackPanel>
                        </Button>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
